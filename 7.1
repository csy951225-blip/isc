#Q1


b = int(input("base? "))
p = int(input("power? "))
r = b ** p
rb = (str(b)+"*") * p
print(f"{rb}={r}".replace("*=","="))

#Q2
t = 0
n = 1
term = 1

while term >= 0.000001:
    term = 1 / (n ** 2)
    t += term
    n += 1

print(f"Estimated sum: {t}")

#Q3

while True:
    try:
        a = float(input("Enter a: "))
        b = float(input("Enter b: "))
        c = float(input("Enter c: "))

        d = b ** 2 - 4 * a * c


        if not a == 0 and d >= 0:
            r1 = (-b - d**0.5) / (2 * a)
            r2 = (-b + d**0.5) / (2 * a)
            print(f" {r1} and {r2}")
            break
        elif not a == 0 and d == 0:
            r1 = (-b ) / (2 * a)
            print(f" {r1}")
            break
        elif not a == 0 and d <0:
            print("no answer")
            break
    except:
        print("Please enter numbers only.")
#Q4
def f(x): 
    return (x ** 2) / 2

dx = 3 / 6 

area = sum((i * dx) * dx for i in range(1, 7))
true_area = 4.5  

print( {area})
print( {true_area})
print( {abs(true_area - area)})
